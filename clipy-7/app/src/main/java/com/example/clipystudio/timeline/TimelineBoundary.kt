package com.example.clipystudio.timeline

import com.example.clipystudio.data.TimelineEngine

typealias TimelineInteractionEngine = TimelineEngine

object TimelineBoundary {
  const val Ownership = "Timeline math, clip placement, snap, scroll, trim, touch slop, and gesture helpers."
}
