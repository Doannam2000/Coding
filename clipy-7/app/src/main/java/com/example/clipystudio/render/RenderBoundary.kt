package com.example.clipystudio.render

import com.example.clipystudio.data.RenderPipelineEngine

typealias RenderPreparationEngine = RenderPipelineEngine

object RenderBoundary {
  const val Ownership = "Export settings, render preparation, effect and transition contracts, and export orchestration."
}
