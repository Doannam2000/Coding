[project]
name = "claude-code-discord-py"
version = "0.1.0"
description = "Python migration baseline for claude-code-discord"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "discord.py>=2.4.0",
  "python-dotenv>=1.0.1",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
